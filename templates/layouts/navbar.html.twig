<nav class="navbar-expand-lg navbar {% if app.request.get('_route') == 'default' %}navbarDefault nav-scroll{% endif %}">
   <div class="container-fluid nav-trigger">
      <a class="navbar-brand" href="{{ path('default') }}">
         <img class="navbar-logo" src="{{ asset('pictures/logo/logo.png') }}" alt="">
         Test Technique
      </a>
      <button class="navbar-toggler" type="button" data-mdb-toggle="collapse"
              data-mdb-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="true"
              aria-label="Toggle navigation">
         <i class="fas fa-bars"></i>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
         <ul class="navbar-nav ms-auto me-3 nav-right">
            {% if app.user %}
               <li class="nav-item">
                  <span class="nav-link">Bienvenue, {{ app.user.lastname }} {{ app.user.firstname }}</span>
               </li>
               {% if is_granted('ROLE_ADMIN') %}
                  <li class="nav-item">
                     <a class="nav-link" href="{{ path('admin_index') }}">Administration</a>
                  </li>
               {% endif %}
               <li class="nav-item navbar-logout">
                  <a class="nav-link" href="{{ path('logout') }}">
                     <i class="fas fa-power-off i-logout"></i>
                     <span class="logout">Déconnexion</span>
                  </a>
               </li>
            {% else %}
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('login') }}">Se connecter</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('register') }}">S'inscrire</a>
               </li>
            {% endif %}
         </ul>
      </div>
   </div>
</nav>
